<UserControl x:Class="Test_WPF.ChallengeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="450" Width="240" xmlns:my="clr-namespace:Test_WPF">
    <Grid Loaded="Grid_Loaded">
        <Grid.Background>
            <ImageBrush Stretch="Uniform" ImageSource="/Test_WPF;component/Images/path879.png"></ImageBrush>
        </Grid.Background>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="2" ShadowDepth="3" RenderingBias="Performance" />
        </Grid.Effect>

        <Label Content="Défis" Height="Auto" HorizontalAlignment="Left" Margin="12,40,0,0" Name="label1" VerticalAlignment="Top" FontSize="25" />
        <Grid Margin="12,112,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="100"></RowDefinition>
                <RowDefinition Height="*" MinHeight="100"></RowDefinition>
            </Grid.RowDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Visible" Width="Auto" Grid.Row="0">
                <StackPanel CanHorizontallyScroll="False" CanVerticallyScroll="False" Name="slistNewDuals" >
                    <Label Content="À relever" Height="Auto" Name="label2" FontSize="20" />
                </StackPanel>
             </ScrollViewer>
            <GridSplitter VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Height="5" Background="Black"/>
            <ScrollViewer VerticalScrollBarVisibility="Visible" Width="Auto" Grid.Row="1">
            <StackPanel CanHorizontallyScroll="True" CanVerticallyScroll="True" Name="listPastDuals">
                <Label Content="Passés" Height="Auto" Name="label3" FontSize="20" />
            </StackPanel>
</ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
